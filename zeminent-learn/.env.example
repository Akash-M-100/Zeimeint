# The public base URL of this app — used to construct post-OAuth redirects
# and other absolute redirects from middleware / route handlers.
# Leave unset in local dev (falls back to request.url).
# In production behind a reverse proxy, set this to the public origin, e.g.:
#   APP_URL=https://learning.zeminent.com
APP_URL=
